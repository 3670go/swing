# Presentation diagram prompts

Generator: built-in image_gen. PPT and application code were not modified.

## Image 1

Create ONE polished standalone Korean presentation diagram image, landscape 16:9, 1920x1080 if possible. Flat vector-like editorial diagram, pure white background, charcoal #202321 text, muted mint-green #6FC394 highlights, very light gray panels, generous whitespace, crisp thin arrows, bold clean Korean sans-serif typography. No photos, no 3D, no gradients, no decorative illustrations, no technology logos, no dense paragraphs. This is for a bootcamp presentation about a chat-based golf coaching service. Minimize text and prioritize obvious data flow. All Korean text must be spelled accurately. Title: "단기 메모리 · 장기 메모리". Top-right small badge: "확장 설계". One clean left-to-right diagram. Left input small card "새 대화". Center two stacked equal large cards: top labeled "단기 메모리" with subtitle "현재 대화 · 최근 질문"; bottom labeled "장기 메모리" with subtitle "회원별 고민 · 목표 · 이전 시도". The arrow from 새 대화 enters 단기 메모리. A downward arrow from 단기 메모리 to 장기 메모리 is labeled "핵심 정보 추출·갱신". Both memory cards each have a separate arrow into one small right-hand card "필요한 맥락 선택", followed by arrow to "맞춤 응답". Under short-term card tiny badge "대화별", under long-term card tiny badge "세션 간 유지". Bottom single muted note "원본 대화 보존 · 장기 맥락 갱신". Show only these labels. Technical intention: conversation-scoped recent context versus persistent user-specific facts; do NOT imply model training or weights changing. Two kinds of context are selected for a response, not every entire conversation stuffed into LLM. Elegant highly readable visual.

## Image 2

Create ONE polished standalone Korean presentation diagram image, landscape 16:9, 1920x1080 if possible. Flat vector-like editorial diagram, pure white background, charcoal #202321 text, muted mint-green #6FC394 highlights, very light gray panels, generous whitespace, crisp thin arrows, bold clean Korean sans-serif typography. No photos, no 3D, no gradients, no decorative illustrations, no technology logos, no dense paragraphs. This is for a bootcamp presentation about a chat-based golf coaching service. Minimize text and prioritize obvious data flow. All Korean text must be spelled accurately. Title: "채팅 엔진과 LangGraph". Small upper-right badge "역할 구분 · 내부 흐름은 확장 설계". Draw ONE large very light-gray rounded outer container labeled "채팅 엔진" spanning most canvas, containing two lanes separated visually. Left lane header "Java · 서비스" with two vertically stacked cards "대화 입출력" and "인증·기록 저장". Right lane header "Python · AI 처리" containing a green-outlined INNER frame prominently titled "LangGraph". The inner frame contains 3 left-to-right nodes: "맥락 선택" → "문제 정의" → a diamond "정보 충분?". From diamond, green arrow labeled "충분" to node "근거 검색" then node "응답 구성"; gray return branch labeled "부족" to small node "추가 질문". Layout inner flow across two rows if needed to preserve generous readable labels. Both 응답 구성 and 추가 질문 feed a single bottom node in Python lane "응답 반환"; that has clear arrow back toward Java 대화 입출력. From Java 대화 입출력 arrow to first LangGraph node labeled "질문·맥락". Outside container left small user icon labeled "사용자", bidirectional arrow to Java lane. Bottom caption "채팅 엔진 = 대화 전체 관리  /  LangGraph = AI 처리 순서·분기 제어". Crucial hierarchy: LangGraph is INSIDE Python AI processing INSIDE chat engine, not a separate peer system and not an LLM model. Do not use multiple agent icons or suggest every deployed chat already uses this planned RAG flow.

## Image 3

Create ONE polished standalone Korean presentation diagram image, landscape 16:9, 1920x1080 if possible. Flat vector-like editorial diagram, pure white background, charcoal #202321 text, muted mint-green #6FC394 highlights, very light gray panels, generous whitespace, crisp thin arrows, bold clean Korean sans-serif typography. No photos, no 3D, no gradients, no decorative illustrations, no technology logos, no dense paragraphs. This is for a bootcamp presentation about a chat-based golf coaching service. Minimize text and prioritize obvious data flow. All Korean text must be spelled accurately. Title: "전체 데이터 흐름". Upper-right badge "구현 기반 + 확장 설계". Sparse architectural overview, not a complex flowchart. One main row left to right with four rounded blocks: "사용자" subtitle "채팅·영상"; "Java 서비스" subtitle "인증·저장"; "Python AI" subtitle "분석·응답 구성"; "코칭 결과" subtitle "설명·다음 행동". Solid charcoal arrows connect left to right, with small arrow labels "질문" between user and Java, "질문·맥락" between Java and Python, "결과" between Python and right result. Important return: draw a distinct solid line below main row from Python back to Java labeled "구조화된 결과", and route result delivery from Java to 코칭 결과 via a clean overhead solid arrow labeled "검증 후 전달"; do NOT draw direct Python-to-user delivery: if a main-row Python→코칭 결과 arrow would contradict this, omit it and use only the Java overhead delivery. Below Java: a solid-outline cylinder or card "회원 데이터" subtitle "대화·프로필·로드맵", bidirectional solid connection to Java. Below Python: one dashed mint-bordered extension panel titled "확장 설계" with two small adjacent boxes "장기 메모리" and "전문가 RAG", each connected with dashed mint arrows to Python labelled collectively "맥락·근거". Inside Python card small subtle label "LangGraph · 처리 제어". Bottom legend "실선: 구현 기반   점선: 확장 설계". Data ownership belongs to Java; Python produces structured candidate results, Java validates and stores and delivers results. High-level semantic simplicity, no endpoint paths, no AWS logos, no vector DB product brand, no claims of production memory/RAG implementation.
