# Java/Spring Boot JDBC connection to the same product database.
JDBC_DATABASE_URL=jdbc:postgresql://REGION.pooler.supabase.com:6543/postgres
JDBC_DATABASE_USERNAME=postgres.PROJECT_REF
JDBC_DATABASE_PASSWORD=replace-with-database-password
DB_MIGRATION_ENABLED=false
# Existing Alembic-managed DB only: verify revision 0003 before one-time baseline.
DB_BASELINE_ON_MIGRATE=false
DB_BASELINE_VERSION=3

# Supabase Dashboard > Project Settings > API
SUPABASE_URL=https://PROJECT_REF.supabase.co
SUPABASE_SECRET_KEY=replace-with-server-only-sb-secret-key

# Must match the private bucket created by the migration.
SUPABASE_STORAGE_BUCKET=swing-media

# Server-only Gemini credential. Never expose this value to the browser.
GEMINI_API_KEY=replace-with-server-only-gemini-api-key
GEMINI_MODEL=gemini-3.6-flash

# Java public API: Custom GPT Action > Authentication > API key > Bearer.
GPT_ACTION_API_KEY=replace-with-a-long-random-server-only-key
# Shared only by Java and Python internal API.
INTERNAL_API_TOKEN=replace-with-a-different-long-random-server-only-key
AI_PROCESSING_ENABLED=false
AI_PROCESSING_BASE_URL=http://127.0.0.1:8000
# AI_PROCESSING_TIMEOUT_SECONDS must be measured and explicitly set before enabling the client.
# AI_PROCESSING_TIMEOUT_SECONDS=180

# Java public Action downloader: only temporary file hosts issued by ChatGPT.
ACTION_FILE_HOSTS=files.oaiusercontent.com

# Local Vite prototype origins accepted by Java.
CLIENT_CORS_ORIGINS=http://localhost:4173,http://127.0.0.1:4173

# Optional until the product upload limit is approved. Value is bytes.
# MAX_VIDEO_BYTES=104857600
MULTIPART_MAX_FILE_SIZE=250MB
MULTIPART_MAX_REQUEST_SIZE=500MB
